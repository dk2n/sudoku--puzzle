<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.infrolog.elu.android"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="10" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name="dk2n.game.sudoku.Sudoku"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:label="@string/about_title" android:name="dk2n.game.sudoku.About" android:theme="@android:style/Theme.Dialog"></activity>
        <activity android:label="@string/settings_title" android:name="dk2n.game.sudoku.Prefs"></activity>
        <activity android:label="@string/game_title" android:name="dk2n.game.sudoku.Game"></activity>
    </application>

</manifest>
